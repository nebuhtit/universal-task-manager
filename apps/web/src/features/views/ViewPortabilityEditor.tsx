import { useRef } from 'react'; import type { SavedView, ViewSortRule } from '@utm/core'; import { CodeEditor } from '../../components/ui/CodeEditor';
type PortableFormat = 'json' | 'csv' | 'xlsx' | 'ics';
export function ViewPortabilityEditor({ view, rules, sortSource, json, onJson, onApplyJson, onImport, onExport }: { view: SavedView; rules: ViewSortRule[]; sortSource: string; json: string; onJson: (value: string) => void; onApplyJson: () => void; onImport: (file: File) => void; onExport: (view: SavedView, mode: 'definition' | 'results' | 'bundle', format?: PortableFormat, metadata?: boolean) => void }) {
  const input = useRef<HTMLInputElement>(null); const snapshot = { ...view, sortSource, sort: rules.map((rule) => ({ field: rule.expression, direction: rule.direction, nulls: rule.nulls })) };
  return <><details className="query-builder json-editor view-json-editor"><summary>View JSON</summary><div className="details-body"><p className="builder-status">This is the complete SavedView draft. Imported JSON is applied as a template and keeps this view ID.</p><CodeEditor language="json" ariaLabel="View JSON" rows={16} value={json} onChange={onJson} /><div className="builder-actions"><button className="secondary compact-action" onClick={() => onJson(JSON.stringify(snapshot, null, 2))}>Refresh from visual editor</button><button className="secondary compact-action" onClick={onApplyJson}>Apply JSON</button><button className="secondary compact-action" onClick={() => input.current?.click()}>Import as template</button><input ref={input} hidden type="file" accept=".json,application/json" onChange={(event) => { const file = event.target.files?.[0]; if (file) onImport(file); event.currentTarget.value = ''; }} /></div></div></details>
    <details className="query-builder view-export-details"><summary>Export view</summary><div className="details-body"><p className="builder-status">Definitions use JSON. Results can also be opened in spreadsheets or calendar apps.</p><div className="builder-actions"><button className="secondary compact-action" onClick={() => onExport(snapshot, 'definition')}>Definition JSON</button><details className="inline-menu"><summary>Results…</summary><div><button onClick={() => onExport(snapshot, 'results')}>JSON</button><button onClick={() => onExport(snapshot, 'results', 'csv')}>CSV</button><button onClick={() => onExport(snapshot, 'results', 'xlsx')}>Excel</button><button onClick={() => onExport(snapshot, 'results', 'ics')}>iCalendar</button><button onClick={() => onExport(snapshot, 'results', 'ics', true)}>iCalendar + UTM metadata</button></div></details><button className="secondary compact-action" onClick={() => onExport(snapshot, 'bundle', 'xlsx')}>Definition + results Excel</button></div></div></details></>;
}
